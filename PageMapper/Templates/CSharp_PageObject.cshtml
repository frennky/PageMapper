namespace MySeleniumTests
{
    public class @Model.ClassName
    {
        #region Properties

		@foreach(var property in Model.Properties)
		{
        <text>[FindsBy(How = How.@property.LocatorType, Using = "@property.Locator")]
        public IWebElement @property.Name { get; set; }</text>
		}

        #endregion

        #region Constructors
        public @(Model.ClassName)(IWebDriver driver)
            : base(driver)
        {
            Url = "@Model.Url";
            PageFactory.InitElements(driver, this);
        }
        #endregion
    }
}